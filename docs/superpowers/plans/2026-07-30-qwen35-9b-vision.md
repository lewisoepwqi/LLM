# Qwen3.5-9B 启用视觉能力 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给现有 8080 端口的 Qwen3.5-9B llama-server 服务补上多模态投影器，开启视觉能力，API 契约不变。

**Architecture:** 不换主权重、不加容器、不占第二张卡。往现有 `models/Qwen3.5-9B/` 补一个 876 MiB 的 mmproj 文件，compose 的 `command` 加 `--mmproj` 与 `--image-max-tokens` 两组参数。纯文本请求 100% 向后兼容，业务后端可以后续独立接入。回滚 = 注释掉 compose 里 4 行并重启。

**Tech Stack:** docker compose · `ghcr.io/ggml-org/llama.cpp:server-cuda-b10156` · GGUF · bash + curl 冒烟测试

**Spec:** `docs/superpowers/specs/2026-07-30-qwen35-9b-vision-design.md`

## Global Constraints

- **执行环境分裂**：本仓库是配置仓库，**没有 GPU、没有模型文件、没有 llama 容器**。Task 1–3 在本仓库完成并本地验证；**Task 4 必须由操作员在离线 GPU 服务器上手工执行**，agent 不要尝试在本机 `docker compose up`。
- **镜像 build 下限：`b9222`**（bartowski 出这批量化所用的 build；其依赖的 MTP 层自 `b9180` 起支持）。
- **钉死镜像 tag：`ghcr.io/ggml-org/llama.cpp:server-cuda-b10156`**，digest `sha256:4c0ece468721077a8bff8c114f8b0a92a8604ed310c91939fadfe37595a53b9e`（2026-07-30 时与浮动 tag `server-cuda` 同 digest）。
- **mmproj 文件**：`mmproj-Qwen_Qwen3.5-9B-f16.gguf`，`918165952` 字节，来自 `bartowski/Qwen_Qwen3.5-9B-GGUF`（与主权重同仓库）。放在 `models/Qwen3.5-9B/`，容器内路径 `/models/Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf`。**选 f16，不要 bf16。**
- **参数默认档（本次实施的唯一档位）**：`CONTEXT_SIZE=16384`、`LLAMA_PARALLEL=2`、`IMAGE_MAX_TOKENS=3072`。
- **不启用 `--media-path` / `file://`**。后端不保证与 llama 容器同机，图片一律走 base64 data URI。
- **不改 `--image-min-tokens` 和 `--mtmd-batch-max-tokens`**，保持默认、不显式传入。
- **不修改业务后端代码**。本计划只交付服务端能力与 API 契约文档。
- 所有新增注释、文档、提交信息用中文，与仓库现有风格一致。

---

### Task 1: 视觉冒烟测试

先写测试。这一步的价值在于：视觉请求和纯文本请求走**同一个** `/v1/chat/completions`，所以「请求没报错」证明不了任何事——mmproj 没加载时服务可能报错，也可能返回一段看似正常的文本。测试用一张四象限纯色图（左上红/右上绿/左下蓝/右下黄）断言回答里至少命中 2 种颜色，这是模型没真正读到图时不可能瞎猜对的。

真实服务在离线服务器上，本机连不到。所以本任务用三个本地 mock HTTP 服务验证断言逻辑的三条路径（mmproj 未加载 / 模型看不到图 / 正常），**在花掉一趟上服务器的成本之前先证明测试脚本本身是对的**。

**Files:**
- Create: `docker/test_vision.sh`
- Create: `/tmp/mock_llama.py`（临时验证用，不进仓库）
- Modify: `docker/test_api.sh`（末尾追加调用）

**Interfaces:**
- Consumes: 无（本任务是起点）
- Produces: `docker/test_vision.sh` — 读环境变量 `BASE_URL`（默认 `http://127.0.0.1:8080/v1`）与 `MODEL`（默认 `qwen3.5-9b`），视觉栈生效时 exit 0 并打印 `PASS: 视觉栈已生效`，否则 exit 1 并打印 `FAIL: ...`。Task 4 的验收步骤依赖它。

- [ ] **Step 1: 创建 `docker/test_vision.sh`**

内嵌的 base64 是一张 610 字节、224×224 的 PNG，四象限依次为红/绿/蓝/黄（像素已回读校验）。内嵌而非放独立图片文件，是为了让脚本自包含——离线服务器上不必额外传附件。

```bash
#!/usr/bin/env bash
# 视觉冒烟测试：确认 llama-server 真的走通了视觉栈（mmproj 已加载且模型能读到图）。
#
# 为什么不只断言"请求没报错"：视觉请求与纯文本请求走同一个 /v1/chat/completions，
# 纯文本是向后兼容的，所以一个 mmproj 未加载或格式写错的请求也可能返回一段看似正常
# 的文本回复。这里用一张四象限纯色图断言回答里至少命中 2 种颜色 —— 模型没真正读到
# 图时不可能瞎猜对。
#
# 用法：
#   bash docker/test_vision.sh
#   BASE_URL=http://127.0.0.1:8081/v1 bash docker/test_vision.sh
set -uo pipefail

BASE_URL="${BASE_URL:-http://127.0.0.1:8080/v1}"
MODEL="${MODEL:-qwen3.5-9b}"

# 224x224 PNG，四象限：左上红 / 右上绿 / 左下蓝 / 右下黄。610 字节，内嵌以保持脚本自包含。
IMG_B64=""

echo "==> 视觉冒烟测试 (${BASE_URL})"

resp=$(curl -sS -w '\n%{http_code}' "${BASE_URL}/chat/completions" \
  -H "Content-Type: application/json" \
  -d "{
    \"model\": \"${MODEL}\",
    \"messages\": [{\"role\": \"user\", \"content\": [
      {\"type\": \"text\", \"text\": \"这张图被平均分成四个方块。请只回答这四个方块分别是什么颜色，用中文颜色词，不要解释。\"},
      {\"type\": \"image_url\", \"image_url\": {\"url\": \"data:image/png;base64,${IMG_B64}\"}}
    ]}],
    \"max_tokens\": 64,
    \"temperature\": 0,
    \"chat_template_kwargs\": {\"enable_thinking\": false}
  }") || { echo "FAIL: 请求发不出去（服务没起？先看 docker compose logs -f）"; exit 1; }

code=$(printf '%s' "$resp" | tail -n1)
body=$(printf '%s' "$resp" | sed '$d')

if [ "$code" != "200" ]; then
  echo "FAIL: HTTP ${code}"
  echo "$body"
  echo "提示：报 multimodal / mmproj 相关错误 = --mmproj 没配上，或镜像 build 低于 b9222。"
  exit 1
fi

echo "响应：$body"

# 直接在响应体里数颜色词。提示词本身不含任何颜色词，所以不会有假阳性。
hits=0
for c in 红 绿 蓝 黄; do
  case "$body" in *"$c"*) hits=$((hits+1));; esac
done
echo "命中颜色数：${hits}/4"

if [ "$hits" -lt 2 ]; then
  echo "FAIL: 只命中 ${hits} 种颜色（阈值 2），视觉栈可能未真正生效。"
  exit 1
fi

echo "PASS: 视觉栈已生效"
```

- [ ] **Step 2: 创建 mock 服务器用于本地验证**

写到 `/tmp/mock_llama.py`，**不要提交进仓库**：

```python
import http.server, json, sys
MODE = sys.argv[1]; PORT = int(sys.argv[2])

class H(http.server.BaseHTTPRequestHandler):
    def do_POST(self):
        self.rfile.read(int(self.headers.get("Content-Length", 0)))
        if MODE == "nommproj":      # mmproj 未加载
            code, payload = 500, {"error": {"message": "multimodal is not supported by this server", "code": 500}}
        elif MODE == "blind":       # 服务 200 了但模型没读到图
            code, payload = 200, {"choices": [{"message": {"content": "抱歉，我无法查看图片内容。"}}]}
        else:                       # 正常
            code, payload = 200, {"choices": [{"message": {"content": "左上红色，右上绿色，左下蓝色，右下黄色"}}]}
        b = json.dumps(payload, ensure_ascii=False).encode()
        self.send_response(code); self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(b))); self.end_headers(); self.wfile.write(b)
    def log_message(self, *a): pass

http.server.HTTPServer(("127.0.0.1", PORT), H).serve_forever()
```

- [ ] **Step 3: 跑 mmproj 未加载场景，确认测试失败**

```bash
python3 /tmp/mock_llama.py nommproj 18081 & MPID=$!
sleep 1
BASE_URL=http://127.0.0.1:18081/v1 bash docker/test_vision.sh; echo "exit=$?"
kill $MPID
```

Expected: 打印 `FAIL: HTTP 500` 与 `提示：报 multimodal / mmproj 相关错误...`，最后 `exit=1`。

- [ ] **Step 4: 跑「模型看不到图」场景，确认测试也失败**

```bash
python3 /tmp/mock_llama.py blind 18082 & MPID=$!
sleep 1
BASE_URL=http://127.0.0.1:18082/v1 bash docker/test_vision.sh; echo "exit=$?"
kill $MPID
```

Expected: 打印 `命中颜色数：0/4` 与 `FAIL: 只命中 0 种颜色（阈值 2）...`，最后 `exit=1`。这一条证明测试不是只在检查 HTTP 状态码。

- [ ] **Step 5: 跑正常场景，确认测试通过**

```bash
python3 /tmp/mock_llama.py ok 18083 & MPID=$!
sleep 1
BASE_URL=http://127.0.0.1:18083/v1 bash docker/test_vision.sh; echo "exit=$?"
kill $MPID
```

Expected: 打印 `命中颜色数：4/4` 与 `PASS: 视觉栈已生效`，最后 `exit=0`。

- [ ] **Step 6: 把视觉测试串进 `docker/test_api.sh`**

在 `docker/test_api.sh` 末尾（现有 `nvidia-smi --query-gpu=...` 那一行之后）追加：

```bash

echo ""
echo "==> 视觉冒烟测试（确认 mmproj 已加载且模型能读图）"
BASE_URL="${BASE_URL}" MODEL="${MODEL}" bash "$(dirname "$0")/test_vision.sh"
```

`test_api.sh` 开头已有 `set -euo pipefail`，所以 `test_vision.sh` 返回非 0 会让整个 `test_api.sh` 失败退出，无需额外处理。

- [ ] **Step 7: 语法检查两个脚本**

```bash
bash -n docker/test_vision.sh && bash -n docker/test_api.sh && echo "语法 OK"
```

Expected: 打印 `语法 OK`。

- [ ] **Step 8: 提交**

```bash
chmod +x docker/test_vision.sh
git add docker/test_vision.sh docker/test_api.sh
git commit -m "test(视觉): 加视觉冒烟测试，用四象限纯色图断言模型真读到了图

视觉请求与纯文本走同一端点且纯文本向后兼容，只断言"请求没报错"测不出
mmproj 有没有加载。改为断言四象限纯色图的颜色命中数 >= 2。
测试图（224x224/610B PNG）内嵌进脚本，离线服务器不必额外传附件。"
```

---

### Task 2: compose 与 .env 接入 mmproj

`docker compose config` 会把变量替换后的最终参数渲染出来，不需要 GPU 也不需要模型文件，这就是本任务的本地验证手段。

**同时修一个已存在的坑**：committed 的 `docker/.env` 是过期副本——`CONTEXT_SIZE=8192` 且缺 `LLAMA_PARALLEL`，而 `.env.docker.example` 已是 `16384` + `LLAMA_PARALLEL=2`。当前 `docker compose config` 渲染出 `-c 8192 --parallel 2`，即**每槽只有 4096**；塞进 3072 的图后只剩约 1000 token 给正文和输出，视觉会直接不可用。必须一并修正。

**Files:**
- Modify: `docker/docker-compose.yml`（`image:` 行；`command:` 列表）
- Modify: `docker/.env`
- Modify: `docker/.env.docker.example`

**Interfaces:**
- Consumes: 无（配置改动独立于 Task 1）
- Produces: 三个新 `.env` 变量，Task 3 文档与 Task 4 runbook 都引用它们 —— `LLAMA_IMAGE`（镜像 tag）、`MMPROJ_FILE`（相对 `/models` 的 mmproj 路径）、`IMAGE_MAX_TOKENS`（每图视觉 token 上限）。

- [ ] **Step 1: 记录改动前的渲染结果，作为对照基线**

```bash
cd docker && docker compose config > /tmp/compose_before.txt && cd ..
grep -E '^\s+-\s+"?(-c|--parallel|8192|2)"?$|image:' /tmp/compose_before.txt
```

Expected: 看到 `-c` / `"8192"` / `--parallel` / `"2"` 和 `image: ghcr.io/ggml-org/llama.cpp:server-cuda`。这就是上面说的坑的现场。

- [ ] **Step 2: 把 `docker-compose.yml` 的 `image:` 行改成钉死版本的变量**

把这一行：

```yaml
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
```

替换为：

```yaml
    # 镜像 tag 钉死 build 号：浮动 tag server-cuda 每次重新打包拿到的 build 都不确定，
    # 离线环境（docker save/load）下无法复现。b10156 在 2026-07-30 与浮动 tag 同 digest。
    # 视觉能力要求 build >= b9222。
    image: ${LLAMA_IMAGE:-ghcr.io/ggml-org/llama.cpp:server-cuda-b10156}
```

- [ ] **Step 3: 在 `command:` 列表里加视觉参数**

在 `- "--cont-batching"` 这一行之后、`- "--host"` 这一行之前插入：

```yaml
      # ---- 视觉能力（多模态投影器）----
      # mmproj 与主权重同在 bartowski/Qwen_Qwen3.5-9B-GGUF 仓库，放同一个目录。
      # CUDA 后端选 f16（bf16 只差 3MB，但 f16 是常规路径）。默认会 offload 到 GPU。
      # 临时关掉视觉：注释掉下面这 4 行，docker compose up -d 即退回纯文本，
      # mmproj 文件留在磁盘上无副作用。
      - "--mmproj"
      - "/models/${MMPROJ_FILE:-Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf}"
      # 每图视觉 token 上限。Qwen-VL 动态分辨率约 784px≈1token，A4 扫描件整页约
      # 1800-2600 token。3072 给整页留足余量，且与每槽 8192 里的正文+输出预算不冲突。
      - "--image-max-tokens"
      - "${IMAGE_MAX_TOKENS:-3072}"
```

- [ ] **Step 4: 修正过期的 `docker/.env`**

把 `docker/.env` 里这两行：

```
# 上下文长度。显存吃紧可降到 4096。
CONTEXT_SIZE=8192
```

替换为（与 `.env.docker.example` 对齐，并补上缺失的 `LLAMA_PARALLEL`）：

```
# 上下文长度（总量）。开并行后按并行数均分到各槽，故 = 单槽上下文 × 并行数。
# 16384 = 2 槽 × 8192/槽。开视觉后每槽要同时装下图片(<=IMAGE_MAX_TOKENS)+正文+输出，
# 不要降到 16384 以下，否则视觉请求会因上下文不够而失败。
CONTEXT_SIZE=16384

# 并行解码槽数（连续批处理）。业务后端 READ_CONCURRENCY 不要超过这里。
LLAMA_PARALLEL=2
```

- [ ] **Step 5: 给 `docker/.env` 追加三个新变量**

追加到 `docker/.env` 末尾：

```

# llama.cpp 镜像，钉死 build 号以便离线重打包时可复现。视觉要求 >= b9222。
LLAMA_IMAGE=ghcr.io/ggml-org/llama.cpp:server-cuda-b10156

# 多模态投影器（视觉能力）。相对 /models 的路径，与主模型同目录。
# 文件在主模型同一个 HF 仓库：bartowski/Qwen_Qwen3.5-9B-GGUF，918165952 字节。
MMPROJ_FILE=Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf

# 每张图的视觉 token 上限（Qwen-VL 动态分辨率约 784px≈1token）。
# 默认档 3072：配 CONTEXT_SIZE=16384 / LLAMA_PARALLEL=2（每槽 8192），
#   整页扫描件够用，且保留 2 路并发。
# 高精度档 6144：须同时把 LLAMA_PARALLEL 改成 1（每槽 16384），单图分辨率上限翻倍，
#   密集小字扫描件 OCR 更准，但并发归零，业务后端 READ_CONCURRENCY 须同步降到 1。
IMAGE_MAX_TOKENS=3072
```

- [ ] **Step 6: 给 `docker/.env.docker.example` 追加同样的三个变量**

把 Step 5 那段完全相同的内容追加到 `docker/.env.docker.example` 末尾。`.env.docker.example` 的 `CONTEXT_SIZE` 和 `LLAMA_PARALLEL` 已经是 16384/2，但注释里没提视觉，把它的 `CONTEXT_SIZE` 注释末尾加一句：

```
# 开视觉后每槽要同时装下图片(<=IMAGE_MAX_TOKENS)+正文+输出，不要降到 16384 以下。
```

- [ ] **Step 7: 用 `docker compose config` 验证渲染结果**

```bash
cd docker && docker compose config > /tmp/compose_after.txt && cd ..
diff /tmp/compose_before.txt /tmp/compose_after.txt
```

Expected diff 恰好包含且仅包含四处变化：
1. `-c "8192"` → `-c "16384"`
2. `image: ...:server-cuda` → `image: ...:server-cuda-b10156`
3. 新增 `--mmproj` + `/models/Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf`
4. 新增 `--image-max-tokens` + `"3072"`

- [ ] **Step 8: 断言渲染出的关键参数正确**

```bash
grep -q 'server-cuda-b10156' /tmp/compose_after.txt \
  && grep -q '/models/Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf' /tmp/compose_after.txt \
  && grep -A1 -- '--image-max-tokens' /tmp/compose_after.txt | grep -q '"3072"' \
  && grep -A1 -- '^      - -c$' /tmp/compose_after.txt | grep -q '"16384"' \
  && echo "compose 渲染 OK"
```

Expected: 打印 `compose 渲染 OK`。

- [ ] **Step 9: 提交**

先提交，下一步的回滚验证要用 `git checkout` 恢复文件，未提交会丢掉 Step 2–3 的改动。

```bash
git add docker/docker-compose.yml docker/.env docker/.env.docker.example
git commit -m "feat(视觉): compose 接入 mmproj，钉死镜像 build，修正过期 .env

- 加 --mmproj + --image-max-tokens 3072（784px≈1token，A4 整页约1800-2600）
- image tag 由浮动 server-cuda 改为钉死 server-cuda-b10156，离线重打包可复现
- 修 docker/.env 过期：CONTEXT_SIZE 8192->16384 并补上缺失的 LLAMA_PARALLEL=2。
  原状态渲染出 -c 8192 --parallel 2 即每槽仅 4096，装不下 3072 的图+正文+输出。"
```

- [ ] **Step 10: 验证回滚路径可用**

把视觉相关的 4 行注释掉，确认渲染确实回退到纯文本参数，再用 `git checkout` 恢复
（此时恢复的是 Step 9 已提交的带视觉版本）：

```bash
cd docker
sed -i 's|^      - "--mmproj"|#      - "--mmproj"|; s|^      - "/models/${MMPROJ_FILE|#      - "/models/${MMPROJ_FILE|; s|^      - "--image-max-tokens"|#      - "--image-max-tokens"|; s|^      - "${IMAGE_MAX_TOKENS|#      - "${IMAGE_MAX_TOKENS|' docker-compose.yml
docker compose config | grep -c 'mmproj'
cd .. && git checkout docker/docker-compose.yml
```

Expected: `grep -c` 输出 `0`（注意 `grep -c` 数到 0 时退出码为 1，这是正常的，看输出的数字即可）。
`git checkout` 之后 `git status` 应是 clean。

---

### Task 3: 文档

**Files:**
- Modify: `docker/README.md`
- Modify: `docker/OFFLINE.md`

**Interfaces:**
- Consumes: Task 2 产出的 `LLAMA_IMAGE` / `MMPROJ_FILE` / `IMAGE_MAX_TOKENS` 三个变量名；Task 1 产出的 `docker/test_vision.sh`
- Produces: 业务后端接入视觉时依赖的 API 契约文档（README 的「视觉能力」章节）

- [ ] **Step 1: README 的目录布局补上 mmproj**

在 `## 推荐目录布局` 的代码块里，`Qwen_Qwen3.5-9B-Q5_K_M.gguf` 那一行下面加一行：

```text
      mmproj-Qwen_Qwen3.5-9B-f16.gguf    # 视觉投影器，与主权重同目录
```

同时把 `docker/` 下的文件清单里补上 `test_vision.sh`。

- [ ] **Step 2: README 的「2. 放模型」补上 mmproj 下载**

在 `### 2. 放模型` 的代码块里，现有 `hf download` 那两行之后加：

```bash
# 视觉投影器（876 MiB，与主权重同仓库；不下这个则视觉不可用，纯文本不受影响）
# 有网：hf download bartowski/Qwen_Qwen3.5-9B-GGUF mmproj-Qwen_Qwen3.5-9B-f16.gguf \
#         --local-dir ~/app/LLM/models/Qwen3.5-9B
```

- [ ] **Step 3: README 新增「视觉能力」章节**

插在 `## 常用运维` 之后、`## 调参对照` 之前：

````markdown
## 视觉能力

Qwen3.5-9B 本身支持图文输入。服务已加载 `mmproj-Qwen_Qwen3.5-9B-f16.gguf`（876 MiB），
视觉与文本共用同一个 8080 服务。

### API 契约

**服务端零变更**：还是 `POST /v1/chat/completions`，端口、模型名、`/v1/models`、`/health` 全不变。

**纯文本请求 100% 向后兼容** —— `content` 是字符串时行为与开视觉前完全一致。所以业务后端
不改也能继续跑，视觉可以先上线、后端后续独立接入，不需要同步发版。

传图时把 `content` 从字符串改为 content-part 数组：

```bash
IMG=$(base64 -w0 合同扫描件.jpg)
curl -sS http://127.0.0.1:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d "{
    \"model\": \"qwen3.5-9b\",
    \"messages\": [{\"role\": \"user\", \"content\": [
      {\"type\": \"text\", \"text\": \"提取这张合同扫描件的甲乙方、金额、签订日期\"},
      {\"type\": \"image_url\", \"image_url\": {\"url\": \"data:image/jpeg;base64,${IMG}\"}}
    ]}],
    \"chat_template_kwargs\": {\"enable_thinking\": false}
  }"
```

### 后端接入约束

1. **必须用 base64 data URI。** llama-server 也支持远程 URL，但那是**服务端**去 fetch，
   本服务在内网、取不到图。本部署未启用 `--media-path`，`file://` 也不可用
   （后端不保证与本容器同机，共享挂载卷不成立）。
2. **base64 膨胀约 33%**：2 MB 的 JPEG → 约 2.7 MB 请求体。llama-server 自身不限体积，
   但**若链路上有 nginx 反代，须调 `client_max_body_size`**。这是最容易踩的坑。
3. **`prompt_tokens` 口径变了**，会包含视觉 token。后端若有 token 统计或限流逻辑需重新校准。
4. **支持格式**：JPEG / PNG / BMP / TGA / GIF。**PDF 需后端自行转图**（如 `pdftoppm -jpeg -r 150`）。
5. 图片 token 上限由 `IMAGE_MAX_TOKENS` 控制（默认 3072）。单张图超限会被降采样，
   不会报错，但小字可能糊掉。

### 两档参数

Qwen-VL 动态分辨率约 **784 px ≈ 1 个视觉 token**，A4 扫描件整页约 **1800–2600 token**。

| 档位 | `.env` 配置 | 每槽上下文 | 适用 |
|---|---|---|---|
| 默认档 | `LLAMA_PARALLEL=2` `CONTEXT_SIZE=16384` `IMAGE_MAX_TOKENS=3072` | 8192 | 整页扫描件够用，保留 2 路并发 |
| 高精度档 | `LLAMA_PARALLEL=1` `CONTEXT_SIZE=16384` `IMAGE_MAX_TOKENS=6144` | 16384 | 密集小字 OCR 更准，但并发归零，业务后端 `READ_CONCURRENCY` 须同步降到 1 |

先跑默认档，用真实扫描件测准确率，不够再切高精度档（改 `.env` 两个值 + `docker compose up -d`）。

### 验证

```bash
bash test_vision.sh          # 单独跑视觉冒烟测试
bash test_api.sh             # 全量（含视觉）
```

用一张四象限纯色图断言模型真读到了图，而不只是"请求没报错"。

### 关掉视觉

注释掉 `docker-compose.yml` 里 `--mmproj` / `--image-max-tokens` 那 4 行，
`docker compose up -d`。服务退回纯文本，显存少占约 0.86 GiB，mmproj 文件留在磁盘上无副作用。
````

- [ ] **Step 4: README 调参对照表补两行**

在 `## 调参对照` 表格里追加：

```markdown
| 视觉精度不够 | `.env` 里 `LLAMA_PARALLEL=1` + `IMAGE_MAX_TOKENS=6144`（并发降为 1） |
| 关掉视觉 | 注释掉 compose 里 `--mmproj` / `--image-max-tokens` 4 行 |
| 升级 llama.cpp | `.env` 里改 `LLAMA_IMAGE=...:server-cuda-bNNNNN`（离线需先 save/load 新镜像） |
```

- [ ] **Step 5: README 排错补三条**

在 `## 排错` 列表末尾追加：

```markdown
- **视觉请求报 multimodal 相关错误**：`--mmproj` 没生效。`docker compose config | grep mmproj`
  确认参数渲染出来了，再 `ls -lh models/Qwen3.5-9B/mmproj-*` 确认文件在（应为 918165952 字节）。
- **视觉请求 200 但模型说看不到图**：多半是镜像 build 太老。
  `docker exec Qwen3.5-9B /app/llama-server --version` 确认 >= b9222，否则重打包镜像。
- **视觉请求报上下文不够 / 输出被截断**：每槽上下文 = `CONTEXT_SIZE / LLAMA_PARALLEL`，
  要同时装下图片(<=`IMAGE_MAX_TOKENS`)+正文+输出。降 `IMAGE_MAX_TOKENS` 或降 `LLAMA_PARALLEL`。
- **请求体过大被拒（413）**：base64 比原图大约 1/3。若前面有 nginx 反代，调 `client_max_body_size`。
```

- [ ] **Step 6: OFFLINE.md 产物表加一行**

在「需要准备 3 类产物」的表格里，第 2 行「模型 GGUF」之后插入：

```markdown
| 2b. 视觉投影器 mmproj | 想要视觉能力则必须 | ~876 MB |
```

并把表头那句「需要准备 3 类产物」改为「需要准备 4 类产物」。

- [ ] **Step 7: OFFLINE.md 的 A1 镜像步骤改用钉死 tag**

把 `### A1. 镜像` 的代码块替换为：

```bash
# tag 钉死 build 号，否则每次打包拿到的 build 不确定，离线环境无法复现。
# 视觉能力要求 >= b9222。这个 tag 要与 .env 里的 LLAMA_IMAGE 一致。
docker pull --platform linux/amd64 ghcr.io/ggml-org/llama.cpp:server-cuda-b10156
docker save ghcr.io/ggml-org/llama.cpp:server-cuda-b10156 -o llama-server-cuda-b10156.tar
# 可选压缩：gzip llama-server-cuda-b10156.tar
```

- [ ] **Step 8: OFFLINE.md 的 A2 加 mmproj 下载**

在 `### A2. 模型` 的代码块末尾追加：

```bash
# 视觉投影器（与主权重同仓库）。不要 mmproj 则跳过，纯文本不受影响。
hf download bartowski/Qwen_Qwen3.5-9B-GGUF mmproj-Qwen_Qwen3.5-9B-f16.gguf --local-dir ./gguf
# 产物：./gguf/mmproj-Qwen_Qwen3.5-9B-f16.gguf（918165952 字节）
# 注意选 f16 不要 bf16 —— 两者只差 3MB，但 CUDA 后端走 f16 是常规路径。
```

- [ ] **Step 9: OFFLINE.md 的 B 上传段加 mmproj**

在 `## B. 上传到服务器` 的代码块里，主 GGUF 那行 `scp` 之后加：

```bash
scp ./gguf/mmproj-Qwen_Qwen3.5-9B-f16.gguf  ubuntu@服务器IP:~/app/LLM/models/Qwen3.5-9B/
```

- [ ] **Step 10: OFFLINE.md 的 C3 与完整性校验段加 mmproj**

`### C3. 确认模型就位` 的命令替换为：

```bash
ls -lh ~/app/LLM/models/Qwen3.5-9B/
# 应看到两个文件：
#   Qwen_Qwen3.5-9B-Q5_K_M.gguf        7111487520 字节
#   mmproj-Qwen_Qwen3.5-9B-f16.gguf     918165952 字节
```

`## 校验完整性` 段的两条 `sha256sum` 命令末尾都加上 `mmproj-Qwen_Qwen3.5-9B-f16.gguf`
（打包机侧加相对路径，服务器侧加 `~/app/LLM/models/Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf`）。

- [ ] **Step 11: 同步 README 末尾的「备选：纯 docker run」示例**

那段示例绕开了 compose，如果不同步就会成为一个静默失效的参考。把它替换为：

```bash
docker run -d --name contract_radar_llm --restart unless-stopped \
  --gpus '"device=0"' -p 8080:8080 \
  -v /models:/models:ro \
  ghcr.io/ggml-org/llama.cpp:server-cuda-b10156 \
  -m /models/Qwen3.5-9B/Qwen_Qwen3.5-9B-Q5_K_M.gguf \
  --mmproj /models/Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf \
  --image-max-tokens 3072 \
  -ngl 99 -c 16384 --parallel 2 --cont-batching \
  --host 0.0.0.0 --port 8080 \
  --chat-template-kwargs '{"enable_thinking":false}'
```

- [ ] **Step 12: 检查文档内部一致性**

```bash
grep -rn 'server-cuda' docker/*.md docker/docker-compose.yml docker/.env docker/.env.docker.example
```

Expected: 所有出现处都是 `server-cuda-b10156`，没有残留的裸 `server-cuda`。

```bash
grep -c '918165952' docker/README.md docker/OFFLINE.md
```

Expected: 两个文件都 >= 1，字节数写法一致。

- [ ] **Step 13: 提交**

```bash
git add docker/README.md docker/OFFLINE.md
git commit -m "docs(视觉): README 加视觉能力章节与 API 契约，OFFLINE 加 mmproj 获取流程

README 明确：服务端零变更、纯文本向后兼容、必须走 base64（内网取不到远程 URL，
且未启用 --media-path），并给出默认档/高精度档两档参数对照。
OFFLINE 补 mmproj 下载上传校验，镜像打包改用钉死 tag。"
```

---

### Task 4: 服务器上线（操作员手工执行）

> **本任务不由 agent 执行。** 本仓库所在机器没有 GPU、没有模型文件、没有 llama 容器；
> 真实服务在离线 GPU 服务器上。以下命令由操作员在那台服务器上按顺序执行。

**Files:** 无仓库改动。产出物是验收记录。

**Interfaces:**
- Consumes: Task 1 的 `docker/test_vision.sh`；Task 2 的 compose 与 `.env`；Task 3 的 OFFLINE.md 流程

- [ ] **Step 1: 前置检查 —— 镜像 build 号 与 本地 tag 名（两件独立的事，都要查）**

```bash
docker exec Qwen3.5-9B /app/llama-server --version
docker images | grep llama.cpp
```

**build 号决定视觉能不能用，tag 名决定容器能不能起来。判据不是一回事，必须分开查——
build 号够新不代表 tag 名对得上，反之亦然。**

判据 1（build 号，来自 `--version` 输出）：**>= b9222**。

推论：这批 GGUF 依赖的 MTP 层自 b9180 起支持，**如果现在的服务本来就能正常加载并产出
正常输出，说明镜像已 >= b9180**，mtmd 视觉栈大概率已在其中。所以需要重打包镜像的概率
不高——但这条命令必须真跑，不要靠推断。

**若 build < b9222**：按 OFFLINE.md 的 A1 在有网机器打包 `server-cuda-b10156` 并 `docker load`，
然后继续。**若 build >= b9222 但 < b10156**：可以先不换镜像，用现有 build 试。

判据 2（tag 名，来自 `docker images` 输出）：compose 已把 `LLAMA_IMAGE` 默认值钉死为
`ghcr.io/ggml-org/llama.cpp:server-cuda-b10156`。这台服务器改动前很可能是用**浮动 tag**
`server-cuda` 打包载入的（改动前的 OFFLINE.md A1 就是这么写的），本地镜像库里的 tag 名
大概率是裸的 `server-cuda`、不是 `server-cuda-b10156`。**即便判据 1 已经判定 build 够新、
不需要重打包镜像，只要本地没有 `server-cuda-b10156` 这个 tag 名，`docker compose up -d`
一样会因为找不到本地镜像而尝试联网拉取——服务器无外网，直接启动失败。**

**若本地没有 `server-cuda-b10156` 这个 tag**（只有 `server-cuda` 或其他名字），二选一：
- 给现有本地镜像打个别名（不需要重新下载/打包，最简单）：
  ```bash
  docker tag ghcr.io/ggml-org/llama.cpp:server-cuda ghcr.io/ggml-org/llama.cpp:server-cuda-b10156
  ```
- 或者把 `.env` 里 `LLAMA_IMAGE` 改成本地实际存在的 tag 名（如
  `LLAMA_IMAGE=ghcr.io/ggml-org/llama.cpp:server-cuda`），不使用钉死名。

- [ ] **Step 2: 确认服务器上的 `.env` 与仓库一致**

```bash
cd ~/app/LLM/docker
grep -E 'CONTEXT_SIZE|LLAMA_PARALLEL|IMAGE_MAX_TOKENS|MMPROJ_FILE|LLAMA_IMAGE' .env
```

Expected: `CONTEXT_SIZE=16384`、`LLAMA_PARALLEL=2`、`IMAGE_MAX_TOKENS=3072`、
`MMPROJ_FILE=Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf`、`LLAMA_IMAGE=...b10156`。

服务器上的 `.env` 是本地文件、不受 git 管，**很可能还是旧的**（尤其缺 `LLAMA_PARALLEL`
会导致每槽只有 4096）。缺哪个补哪个。

- [ ] **Step 3: 传 mmproj 文件并校验**

按 OFFLINE.md 流程 scp 上传后：

```bash
ls -l ~/app/LLM/models/Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf
sha256sum ~/app/LLM/models/Qwen3.5-9B/mmproj-Qwen_Qwen3.5-9B-f16.gguf
```

Expected: 大小恰好 `918165952` 字节；sha256 与打包机一致。**不一致不要继续**——
GGUF 损坏表现为加载报错或输出乱码，会浪费大量排查时间。

- [ ] **Step 4: 确认渲染参数，再启动**

```bash
cd ~/app/LLM/docker
docker compose config | grep -E 'image:|mmproj|image-max-tokens|^      - "?(16384|2)"?$'
```

确认 mmproj 路径、`--image-max-tokens 3072`、`-c 16384`、`--parallel 2` 都对了之后：

```bash
docker compose up -d
docker compose logs -f
```

Expected: 日志里出现 mmproj / clip / vision 相关的加载行，且最终 `server listening`。
若日志报找不到 mmproj 文件，回到 Step 3 核对路径。

- [ ] **Step 5: 验收 —— 跑全量测试**

```bash
cd ~/app/LLM/docker
bash test_api.sh
```

Expected: `/v1/models` 返回、纯文本回答正常（这条同时是**纯文本回归验证**）、
`nvidia-smi` 有显存占用、最后 `PASS: 视觉栈已生效`。

- [ ] **Step 6: 验收 —— 抓推理瞬间的显存峰值**

一个终端跑：

```bash
watch -n0.5 'nvidia-smi --query-gpu=index,memory.used,utilization.gpu --format=csv'
```

另一个终端跑 `bash test_vision.sh`，记录 GPU 0 的**峰值**。

预期约 11–12 GiB / 16 GiB（权重 6.62 + mmproj 0.86 + KV/计算 2–3 + 视觉编码峰值 0.5–1）。
**必须抓推理瞬间，不能只看空闲态。** 若峰值逼近 16 GiB：优先降 `IMAGE_MAX_TOKENS`，
其次降 `CONTEXT_SIZE`（同时按比例降 `LLAMA_PARALLEL`）。

- [ ] **Step 7: 验收 —— 真实扫描件准确率**

拿一张真实合同扫描件跑字段抽取（README「视觉能力」章节的 curl 示例，把提示词换成
实际要抽的字段），人工核对甲乙方、金额、签订日期是否正确。

**这一步的产出是决策依据**：默认档 3072 token 够不够读清你们实际 DPI/字号的扫描件，
只能实测。不够就切高精度档（`LLAMA_PARALLEL=1` + `IMAGE_MAX_TOKENS=6144`，
并把业务后端 `READ_CONCURRENCY` 同步降到 1），再重跑 Step 5–7。

- [ ] **Step 8: 记录验收结果**

在 `docker/README.md` 的「视觉能力」章节末尾追加一个「实测记录」小节，填入四项实测值
（无需猜测，全部来自前面步骤的实际输出），然后提交：

```markdown
### 实测记录（2026-XX-XX 上线）

- llama-server build：`bXXXXX`
- 推理峰值显存：`XX.X GiB / 16 GiB`（GPU 0，抓自 test_vision.sh 执行瞬间）
- 真实合同扫描件字段抽取：甲乙方 / 金额 / 签订日期 — 正确或错误情况
- 最终采用档位：默认档（`IMAGE_MAX_TOKENS=3072` / `LLAMA_PARALLEL=2`）
```

这四项是后续调参和排查的唯一事实基础，别省。

---

## 回滚

```bash
cd ~/app/LLM/docker
# 注释掉 docker-compose.yml 里 --mmproj / --image-max-tokens 那 4 行
docker compose up -d
bash test_api.sh    # 视觉冒烟会失败，这是预期的；前面的纯文本检查应全过
```

mmproj 文件留在磁盘上无副作用，随时可以取消注释恢复。
